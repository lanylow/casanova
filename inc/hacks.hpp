#pragma once

namespace casanova::hacks {
  namespace speedhack {
    void update();
  }

  namespace display {
    void update_fullscreen();
    void update_vsync();
    void update_fps_bypass();
  }
}